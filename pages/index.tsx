export default function Home() {

  const message : string = "Hello World";
  // Initialize [addEventListenerOptions] from tyypscript [lib] defined in [tsconfig.json] 
    let a : AddEventListenerOptions;
  // Initialize [addEventListenerOptions] from tyypscript [lib] defined in [tsconfig.json] 
  // Interface with [NodeJS.Process] type
    
    let b : NodeJS.Process
    // Disabling code that causes error when [tsconfig.compilerOptions.strict] is set to true
      //console.log(b.browser);
    // Disabling code that causes error when [tsconfig.compilerOptions.strict] is set to true
    
  // Interface with [NodeJS.Process] type

  let person : Person = {
    name : 'Fred'
  };

  return (
    <div>
      {message}
    </div>
  );

};
